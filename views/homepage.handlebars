{{> navbar}}

<h1 class="text-center">Homepage</h1>

<div>
    {{#each allBlogs as |blog|}}
    <div  class="card-header justify-space-between">
        <h1 class="title">{{ blog.title }}</h1>
        <p class="author">Posted by: {{ user.username }}</p>
        <h5>{{blog.createdAt}}</h5>
    </div>
    <div class="card-body">
        <p>{{blog.content}}</p>
    </div>
    <div>
        <h4>
            {{!-- <a href="/api/comments/:blog.id">Comments</a> --}}
            <a href="/api/comments/{{ blog.id }}">Comments</a>
        </h4>
    </div>
    {{/each}}
</div>

{{!-- i would start by testing that a frontend comments page can render with no data then add the backend 
call to the js and then add the returned data to the front end --}}
{{!-- add a route for /comments/:blogId
5:01
get it to just output the blogId requested
New
5:02
then update comments.js to pull the data --}}


