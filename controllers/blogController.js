const {
    Blog,
    Comment
} = require('../models');

module.exports = {
    // create blog
    // get all blogs
    // single blog

    // findBlog: (req, res) => {
    //     // TODO - req.query.id

    //     const blogId = req.query.id || req.params.id;

    //     Blog.findOne({
    //         where: {
    //             id: blogId,
    //         },
    //         include: Comment
    //     })
    // }
};

// /blog/${id}